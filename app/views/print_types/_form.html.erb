<%= form_for(@print_type) do |f| %>
  <% if @print_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@print_type.errors.count, "error") %> prohibited this print_type from being saved:</h2>

      <ul>
      <% @print_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :display_order %><br>
    <%= f.number_field :display_order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
